# Deprecated Routes

このディレクトリには、使用されなくなったルータファイルが格納されています。

## ⚠️ 警告

**このディレクトリのファイルは `app/main.py` で一切 import されておらず、アプリケーション実行時には読み込まれません。**

## 隔離日: 2025-11-10

## 🗑️ 完全削除予定日: 2025-12-31

1スプリント（約6週間）の監視期間を経て、問題がなければ完全に削除します。

---

## 削除理由

以下のルータファイルは `app/main.py` で登録されておらず、他のファイルからもimportされていないため、未使用と判断しました。

| ファイル | 削除理由 | 代替ルート |
|---------|---------|-----------|
| `alerts.py` | main.py で未登録、参照なし | なし（機能未使用） |
| `shipping.py` | main.py で未登録、参照なし | なし（機能未使用） |
| `orders.py` | `orders_refactored.py` に移行済み | `app/api/routes/orders_refactored.py` |
| `masters_bulk_load.py` | `masters.py` に統合済み | `POST /api/masters/bulk` |
| `masters_customers.py` | `masters.py` に統合済み | `GET/POST/PUT/DELETE /api/masters/customers` |
| `masters_products.py` | `masters.py` に統合済み | `GET/POST/PUT/DELETE /api/masters/products` |
| `masters_suppliers.py` | `masters.py` に統合済み | `GET/POST/PUT/DELETE /api/masters/suppliers` |
| `masters_warehouses.py` | `masters.py` に統合済み | `GET/POST/PUT/DELETE /api/masters/warehouses` |

---

## 検証済み事項

- ✅ `app/main.py` で import されていないことを確認
- ✅ `app/api/routes/__init__.py` で export されていないことを確認
- ✅ 他のファイルから参照されていないことを確認（grep検索実施）
- ✅ Lint/型チェックが全て PASS することを確認
- ✅ API I/F に破壊的変更がないことを確認

---

## 復元方法

万が一、これらのファイルが必要になった場合は、以下のコマンドで復元できます：

```bash
# 個別ファイルの復元
git mv backend/deprecated/routes/<filename> backend/app/api/routes/

# 例: orders.py を復元
git mv backend/deprecated/routes/orders.py backend/app/api/routes/

# 復元後は app/api/routes/__init__.py と app/main.py で登録が必要
```

---

## 削除タイムライン

| 日付 | アクション |
|------|----------|
| 2025-11-10 | deprecated/routes に隔離 |
| 2025-11-24 | 2週間経過 - 問題報告の確認 |
| 2025-12-08 | 4週間経過 - 最終確認 |
| 2025-12-31 | **完全削除実施**（問題なければ） |

---

## 関連ドキュメント

- [リファクタリングレポート](../../docs/architecture/refactor_20251110.md)
- [コードベース構造](../../docs/architecture/codebase_structure.md)
- [Backend README](../README.md)

---

## 連絡先

削除に関する質問や復元が必要な場合は、開発チームに連絡してください。
